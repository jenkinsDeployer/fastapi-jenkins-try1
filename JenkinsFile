pipeline {
    agent any

    environment {
        VENV_DIR = "venv"
    }

    stages {
        stage('Clone Repo') {
            steps {
                git credentialsId: 'github-creds', url: 'https://github.com/YOUR_USERNAME/fastapi-jenkins-demo.git'
            }
        }

        stage('Set Up Python Env') {
            steps {
                bat "python -m venv %VENV_DIR%"
                bat ".\\%VENV_DIR%\\Scripts\\activate && pip install --upgrade pip"
                bat ".\\%VENV_DIR%\\Scripts\\activate && pip install -r requirements.txt"
                bat ".\\%VENV_DIR%\\Scripts\\activate && pip install pytest"
            }
        }

        stage('Run Unit Tests') {
            steps {
                bat ".\\%VENV_DIR%\\Scripts\\activate && pytest tests/"
            }
        }
    }
}

